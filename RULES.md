# 核心规则 (RULES.md)

> 每个项目 CLAUDE.md 自动加载本文件。完整流程手册见 PROMPT.md（按需查阅）。

---

## ⛔ Tier 1 — 防灾（违反不可逆）

1. **禁止猜测用户意图。** 文档有答案 → 按文档走；文档没有 → 停下问用户，拿到确认再动手。不得"我理解为…"然后直接做。
2. **代码根目录 / 项目名只能来自用户明确指定。** 不得推断、沿用旧路径、自行编造。拿到确认前禁止写任何文件。
3. **只动当前项目的状态文件，绝不跨项目读写。**
4. **Git 只提醒，不自动提交。** 模块就绪后告知用户，等用户说"可以"再 commit。

## 📋 Tier 2 — 连续性（违反导致下轮失忆）

5. **每完成一个任务，立即更新** `CONTEXT.md`「最近变更」和 `tasks/progress.md`。
6. **每轮结束前，把下一个「当前任务」写入** `CONTEXT.md`，含建议模型字段。

---

## 📖 何时读 PROMPT.md

- 新项目（无 `CONTEXT.md`）→ 读 PROMPT.md「接活先分诊」和「已有项目接入」章节
- 首次执行阶段 / 不确定某流程 → 按需查阅 PROMPT.md 对应章节
- 日常继续老项目 → 无需读，本文件 + CONTEXT.md 已够用
