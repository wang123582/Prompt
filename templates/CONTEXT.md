# 项目上下文 (CONTEXT.md)
> 本项目**自己的状态文件**（每个项目一份，互不共享）。AI 每次启动必读（操作规则见中央 `PROMPT.md`）。
> 执行完成后，AI 必须更新「当前进度」和「最近变更」区块。
> ⚠️ 这是**状态**，不是规则；规则由全局 `~/.claude/CLAUDE.md` 自动加载（只读），不要改规则。

---

## 项目概述

<!-- 一段话描述项目目标 -->

## 代码根目录

> ⚠️ 代码写在**项目自己的代码目录**里。子 agent 执行前先 `cd` 到该目录。
> 本状态文件就在**本项目代码根目录**下，与代码同处一地，随项目一起移动。

- 本项目代码根目录：<!-- 即本文件所在目录，填绝对路径供子 agent `cd` 用 -->

---

## 文档索引

| 文档 | 说明 | 何时读 |
|------|------|--------|
| 全局 `RULES.md` | 核心规则，开会话自动加载（只读） | 自动 |
| 中央 `PROMPT.md` | 完整运行手册，与 `RULES.md` 同目录（只读） | 流程不确定时 |
| 本目录 `design.md` | 需求 + 详细设计 + ADR | 开始新模块时 |
| 本目录 `USAGE.md` | 使用 / 构建 / 修改参数手册 | 要跑/构建/调参时 |
| 本目录 `tasks/progress.md` | 模块级总进度 checklist | 每次执行前确认进度 |
| 本目录 `tasks/<模块>.md` | 每模块最小子任务 | 实现该模块时 |
| 本文件 `CONTEXT.md` | 本项目当前状态 | 每次必读 |

---

## 当前进度

### 主流程
- [ ] 需求确认
- [ ] 详细设计
- [ ] 任务划分
- [ ] 模块实现（见 tasks/progress.md）
- [ ] 集成测试

### 新功能
<!-- 格式：- [ ] feature: <功能名> -->

### Bug 修复
<!-- 格式：- [ ] bugfix: <描述> -->

---

## 当前任务

<!-- 通常由上一轮 AI 收尾时自动写入；留空时 AI 依据「当前进度」+ tasks/progress.md 自行判断。 -->

**任务类型：**
**任务描述：**
**涉及模块：**
**参考文档：**

---

## 待确认问题

<!-- 默认模式：意图有歧义时写在这里并停下等回复。夜间模式：记下 + 用最稳妥假设继续。 -->

_（当前无待确认问题）_

---

## 最近变更

<!-- 格式：- YYYY-MM-DD: [模块名] 做了什么，有什么关键决策或注意事项 -->
