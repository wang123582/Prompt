# 运行手册 (PROMPT.md)

> 这是给 AI 看的操作说明，本身是**只读规则**（已加系统级保护，改不动也别改）。
> 用户说「读 `/home/toe/prompt` 文件夹，开始干活」→ 按本手册执行。
> 用户说「进入夜间模式」→ 切到下方「夜间模式」。

---

## ⛔ 最高优先级：禁止猜测，必须提问

> **这条规则凌驾于所有自动化、所有「最稳妥假设」之上（夜间模式除外）。**

- **不明确时，先读文档，再动手，最后才问用户。** 顺序：① 重读本手册相关章节；② 读 `CONTEXT.md` / `design.md`；③ 文档确实没有覆盖时，才停下来向用户提问。
- **文档覆盖了的情况，直接按文档走，不得再问用户。** 问已有答案的问题是在浪费 token。
- **不要猜测用户意图。** 文档也没有答案时——需求边界、路径、模块范围、行为预期——**必须停下来向用户提问**，拿到明确回答后再动手。
- **不要用「我理解为…」然后直接做。** 先说「我理解为…，对吗？」，等确认。
- **每一次猜错、每一次不必要的提问都在浪费用户的 token 和时间。**
- 唯一例外：夜间模式（用户已明确开启），此时才允许用最稳妥假设推进并记录。

---

## 目录结构与状态位置（先搞清「规则」和「状态」分家）

- **规则 / 模板（中央，只读）**：`/home/toe/prompt/` 下的 `PROMPT.md`(本文件)、`CLAUDE.md`、`templates/`。所有项目共用，**禁止修改**。
- **状态（每项目一份，可写）**：该项目**代码根目录**下的 `CONTEXT.md`、`design.md`、`USAGE.md`、`tasks/`。**不在 `/home/toe/prompt/` 下**。
- **代码**：同样写在该项目**代码根目录**，**绝不写进 `/home/toe/prompt`**。
- ⚠️ **只动当前项目代码根的状态文件**，绝不跨项目读写——这是上一版"读串了别项目 CONTEXT"的根因。
- 当前是哪个项目：由该项目代码根的 `CLAUDE.md` 决定（它 `@import` 本手册 + 代码根的 `CONTEXT.md`）。若直接在 `/home/toe/prompt` 开会话且不明确，**先问用户是哪个项目**。

---

## 硬约束：代码根目录 / 项目名 必须用户确认，禁止假设

> 这条优先级高于一切自动化。违反它造成的乱写是本系统出过的真实事故。

- **代码根目录、项目名（`projects/<项目名>`）只能来自用户明确指定**。agent **不得自行编造、推断，也不得"沿用某个看起来像的目录名"**（例如从某个路径反推一个项目名）。
- **拿到明确代码根目录之前**：禁止写任何代码、禁止 `mkdir projects/<名>/`、禁止执行「第 0 步初始化」。默认模式 → 停下问用户；夜间模式 → 标 `⚠️ blocked`，**绝不猜一个路径硬写**。
- 即便上一轮会话、或某个旧文件里出现过某路径/某名字，也要**与用户当面确认后**才采用；确认前在「待确认问题」标注「此路径/项目名疑似假设，待确认」。
- 项目名只是 `projects/` 下的目录标识，**用户怎么定就怎么用**，agent 无权擅自命名或改名。

---

## 接活先分诊（开工第一步：先判断「这是什么活」，再决定怎么干）

**别一进来就拷模板。** 先看本项目**代码根**是否已有 `CONTEXT.md`（状态文件）：

**A. 已有状态（老项目继续）** → 跳过初始化，直接走下面「启动步骤」，按 `CONTEXT.md`「当前任务」干。具体是 debug / 新功能 / 执行，由「当前任务」里的任务类型决定（见末尾「当前任务填写参考」）。

**B. 没有状态（首次）** → 先确认属于哪类，再决定要不要初始化、走哪条流水线：

| 类别 | 判据 | 怎么走 |
|------|------|--------|
| ① 全新从零项目 | 还没有可用代码 | 先做「初始化」（见「已有项目接入」第 0 步）→ 再从「阶段流水线」① 需求确认开始 |
| ② 已有代码、首次接入本系统 | 代码已存在、但没本系统文档 | 先做「初始化」→ 再走「已有项目接入」增量盘点，**不从 ① 需求重做** |
| ③ 一次性 / 临时任务 | 回答问题、跑个脚本、看一眼代码 | **不初始化、不进流水线**，干完即走 |
| ④ 说不清 | — | 问用户；夜间模式取最稳妥假设：有代码 → ②，无代码 → ① |

→ 类别定了，才执行（或跳过）「第 0 步初始化」。

---

## 启动步骤

> 适用：分诊为 A，或 B 完成「初始化」之后。
> 「本项目」= 当前项目代码根 `CLAUDE.md` 指向的那个 `projects/<项目名>/`。下面所有 `CONTEXT.md`/`design.md`/`tasks/` 都指**本项目自己那份**。

1. 读**本项目** `CONTEXT.md`，了解全貌、当前阶段、代码根目录（多数情况它已被项目 `CLAUDE.md` 自动加载）。
2. 按「文档索引」按需读**本项目** `design.md`、`tasks/`。
3. 找到**本项目** `CONTEXT.md`「当前任务」，开始执行。
4. 「当前任务」为空时，依据「当前进度」+ 本项目 `tasks/progress.md` 自行判断下一个任务；判断不了就问用户（夜间模式则按最稳妥假设走）。
5. **模型提醒（开工前必做）**：读取「当前任务」里的 `建议模型` 字段，在回复**第一句**告知用户：「本任务建议使用 `<模型>`，请确认当前模型是否正确（`/model` 切换）。」若字段为空则跳过。

---

## 工作模式

### 默认模式（白天 / 自治）
- **默认全自动推进**：做完一个子任务接着下一个，不必每步请示。
- **唯一停下来的理由**：对**用户的真实意图**有歧义、无法判断方向。此时把问题写进「待确认问题」并停下等回复。**绝不靠猜；任何假设都必须先告知。**
- **技术细节自己定**：命名、目录、用哪个库等自行按最佳实践决定；**重要技术选型**在 `design.md` 第四章追加一条 ADR。
- 用户确认后清空「待确认问题」，继续。

### 夜间模式（用户说「进入夜间模式」时启用）
- **完全无人值守，不为任何疑问停下等待。**
- 遇歧义：① 在「待确认问题」记录；② 用**最稳妥假设**继续，或跳过该任务做下一个；③ 在「最近变更」标注做了什么假设。
- **持续推进** `tasks/` 里的任务，直到全部完成或剩下的都 `blocked`。
- 每完成一个子任务**立即更新文档**。
- **不提交 git**；收尾时在「最近变更」写当晚汇总（做了什么 / 哪些假设 / 哪些 blocked / **哪些模块已就绪待提交**），等用户早上裁决。

---

## 阶段流水线（每阶段一个会话）

| 阶段 | 输入 | 产出 | 建议模型 | 自动程度 |
|------|------|------|---------|---------|
| ① 需求确认 | 用户口述 | `design.md` 第一章 | Sonnet | 半自动（意图歧义会问） |
| ② 详细设计 | design.md ①章 | `design.md` 第二章 + ADR | Sonnet（架构难题可 Opus） | 半自动 |
| ③ 任务划分 | design.md | `tasks/<模块>.md` + `tasks/progress.md` | Sonnet | 全自动 |
| ④ 执行 | design.md + tasks/ | 代码+测试，勾 progress | 主 Opus/Sonnet；子 agent 见下 | 自动（主+并行子 agent） |
| ⑤ Review | 代码 | 审查/修复 | 复杂逻辑 Opus；日常 Sonnet；风格 Haiku | 自动 |

- **`/clear` 和 `/model` 切换是用户手动的**，我无法在一个会话里自动完成。每阶段收尾时，我会提醒用户「请 `/clear` 后切到 <模型> 做下一阶段」。
- **章节互不删**：① 阶段只写/改 `design.md` 第一章，② 阶段只写/改第二章，**禁止删除对方章节内容**。

---

## ④ 执行阶段机制（主 Agent + 并行子 Agent）

1. **主会话 = 进度跟踪器**：读 `tasks/progress.md`，挑出未完成且**相互独立**的模块。
2. **并行派生子 agent**：每个独立模块开一个 **general-purpose 子 agent（工具全开，可 Read/Write/Bash/跑测试/git）**，指定 `model: sonnet`。喂给它 `design.md` 对应章节 + `tasks/<模块>.md`。
3. **子 agent**：`cd` 到代码根目录 → 实现 → 写 pytest → **实际运行** `pytest`/`mypy`/`ruff check` → 把命令输出报回主会话。
4. **主会话**更新 `tasks/progress.md` 和 `tasks/<模块>.md`，循环下一批。
5. **并行前提是模块真解耦**：若多个模块要改**同一个共享文件**（如公共 config），那部分**先串行做掉**，再并行其余，避免撞车。
6. **调试模型**：默认 **Sonnet**；只有 lint/typo 级简单 bug 用 Haiku；**并发 / 跨多文件的难 bug 升 Opus**（Haiku 啃不动硬 bug）。

---

## 已有项目接入（增量盘点，不全量）

接入一个**已有代码、没用过本系统**的项目时，**不要一次性通读整个代码库**。改成**按需增量**：

0. **先初始化本项目（最优先做）** —— 仅在「分诊」判为 **B① 全新项目 / B② 已有代码首次接入** 时执行；分诊为 A（老项目）或 ③（一次性任务）**跳过本步**。两步：
   - **建状态目录**：在**代码根目录**下执行 `mkdir -p tasks`，从 `templates/` 拷入 `CONTEXT.md`、`design.md`、`USAGE.md`、`tasks/progress.md`、`tasks/_模块模板.md`，并在 `CONTEXT.md` 仅填上「代码根目录」一项。⚠️ **只建骨架，禁止根据记忆、已有会话上下文或先验知识预填任何内容**——`design.md`、`USAGE.md`、`tasks/progress.md` 全部留空模板，等任务涉及某模块时再按需增量填入。
   - **建项目 CLAUDE.md**：把 `templates/project-CLAUDE.md` 拷到该项目**代码根目录**、改名 `CLAUDE.md`，将 `@import` 中 `CONTEXT.md` 的路径改为 `@<代码根目录>/CONTEXT.md`（本地路径，不是 `/home/toe/prompt/projects/...`）。这样以后**任何 agent**一进该代码目录都会自动加载中央规则 + 本项目状态。代码根已有 `CLAUDE.md` 的，把模板里的 `@import` 两行和「红线」段**追加**进去，别覆盖原内容。
1. 只在当前任务**真正需要**时，读相关模块的文件。
2. 读懂后，把该模块的**现状**补进 `design.md` 第二章（标注「现状」，不是理想需求）、在 `tasks/progress.md` 登记该模块、必要时在 `USAGE.md` 补使用/构建/参数。
3. 没碰到的模块**先不管**，等任务涉及再补。文档是边做边长出来的。
4. 老代码的 pytest/mypy/ruff 门禁**只对你今后改动的部分生效**，不强制一次性给历史代码补测（要还债就单独建 `tasks/补测.md`）。
5. 首次接触某代码根目录时，顺手确认有没有 git / pytest / mypy / ruff 配置，缺什么先报给用户。

---

## 停点规则（实现受阻 / 卡住时读）

**触发**：某功能反复实现失败，或判断实现不了，或在同一路径上反复消耗。命中任一停点就**停手**，不要继续烧 token。

| 停点 | 阈值 | 动作 |
|------|------|------|
| 次数停点 | 同一报错/方法连续 **3 次**无新信息 | 停，换方向 |
| 迭代硬上限 | 单个子任务 **≥15 步**工具调用 | 强停 |
| 时间停点 | 单模块/单 bug 卡 **≥20 分钟**无关键进展 | 标 `⚠️ blocked` |
| 相似度停点 | 连续两次尝试产出**几乎雷同** | 判定原地打转，停 |
| 目标停点 | 已拿到「明确下一步 / 够决策的信息 / 当前最合理结论」 | 停止追问，去执行 |

命中后：在 `tasks/progress.md` 把该模块标 `⚠️ blocked`，在「待确认问题」写清**原因 + 已试过的路径**，然后**换方向或上报用户**。

**角色要求**：若判断用户已不是在找新证据、而是在拖延接受某个结论，**直接指出来**，不要顺着情绪把问题越聊越复杂。

---

## 执行规则

**代码：**
- 核心逻辑必须有 pytest 单元测试；硬件/IO/ROS2 接口层用 mock，难单测的标「需硬件验证」。
- 完成后**必须实际运行** `pytest`、`mypy`、`ruff check` 并贴输出；未通过不算完成；无法运行要说明原因，**不得谎称通过**。
- **模块间解耦、可独立测试**（任务划分阶段就要保证这点）。

**范围（读和改都按需，不全量）：**
- **只读当前任务相关的文件**：模块解耦，需要哪个模块就读哪个模块的文件，**不要一上来全量扫整个代码库**。
- 只改动当前任务相关模块，不顺手改无关代码，不过度设计。
- 文档冲突时以 `design.md` 为设计权威；影响当前任务时停下确认（夜间则按 design.md 走并记录）。

---

## Git（只提醒，不自动提交）

- 一个模块测试通过后，**提醒用户**：「模块 X 已就绪，可提交」，**等用户说「可以」再 commit**。
- 用户批准后：在**代码仓库**里 commit，信息含模块名 + 测试结果，末尾加
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- **夜间模式不提交**，只在收尾汇总里列出「已就绪待提交」的模块清单。

---

## 文档更新（每完成一个任务必做）

- `tasks/progress.md` 勾选模块进度（可标 `⚠️ blocked`）。
- `tasks/<模块>.md` 勾选已完成子任务。
- `CONTEXT.md`「最近变更」追加一条（日期 + 内容 + 关键决策/假设）。
- **改了影响运行 / 构建 / 参数的代码 → 同步更新 `USAGE.md` 对应小节。**
- 新功能 / bug 修复按 `tasks/` 模板追加对应文件或区块。

---

## 收尾：交接给下一轮

每轮结束前：
1. 把**下一个合理的「当前任务」**（含 `建议模型` 字段）写进 `CONTEXT.md`「当前任务」。
2. 在产出**末尾**用醒目格式提醒用户：「下一阶段建议模型：`<模型>`，请 `/clear` 后执行 `/model <模型>` 再继续。」

这样用户下次只需说「继续」，无需自己查模型。

---

## 「当前任务」填写参考

### 需求确认
```
任务类型：需求确认
任务描述：以提问方式逐步确认项目需求，完成后写入 design.md 第一章。
涉及模块：全部
参考文档：design.md
建议模型：Sonnet（半结构化访谈，无需深度推理）
```
### 详细设计
```
任务类型：设计
任务描述：根据 design.md 第一章，补全第二章各模块详细设计（保证模块解耦、可独立测试）。
涉及模块：全部
参考文档：design.md
建议模型：Sonnet；架构复杂 / 多模块强耦合时升 Opus
```
### 任务划分
```
任务类型：任务划分
任务描述：为每个模块生成最小子任务到 tasks/<模块>.md，并建立 tasks/progress.md。
涉及模块：全部
参考文档：design.md，tasks/
建议模型：Sonnet（结构化拆解，无需深度推理）
```
### 执行某个模块
```
任务类型：执行
任务描述：并行实现未完成的独立模块，各自完成子任务与测试。
涉及模块：<模块列表，或"全部未完成">
参考文档：design.md，tasks/progress.md
建议模型：主会话 Opus/Sonnet；子 agent 默认 Sonnet；见「④ 执行阶段机制」调试模型规则
```
### 新功能
```
任务类型：新功能
任务描述：实现新功能 <功能名>，需求：<一句话>。
涉及模块：待评估
参考文档：design.md，tasks/
建议模型：Sonnet（需求单一）；跨多模块复杂设计升 Opus
```
### 修复 Bug
```
任务类型：修复bug
任务描述：<bug 描述>，复现：<步骤>，报错：<粘贴>。
涉及模块：<已知模块，或"待定位">
参考文档：tasks/progress.md
建议模型：Haiku（lint/typo 级）；Sonnet（常规 bug）；Opus（并发 / 跨多文件难 bug）
```
